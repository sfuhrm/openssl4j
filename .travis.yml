language: ruby

services:
  - docker

jobs:
  include:
  - os: linux
    compiler: gcc
    arch: amd64
  - os: linux
    compiler: gcc
    arch: arm64
  - os: linux
    arch: ppc64le
  - os: linux
    arch: s390x

before_install:
- docker build -t debian-adoptopenjdk .travis
#- docker run -d -p 127.0.0.1:80:4567 carlad/sinatra /bin/sh -c "cd /root/sinatra; bundle exec foreman start;"

script:
- docker run -t -i debian-adoptopenjdk:latest
#  - bash -x .travis/build.sh
